import React from 'react';

const Game = () => {

  // const maxTurns = 10

  // secret code in state

  // generateSecretCode()
    // returns 4 different digits, 0-7,

  // guesses in state

  // calculateFeedback(guess)

  // handleGuess(guess)
    // add guess to guesses
    // compare guess to secret code


  // evaluate for end condition after every guess
    // either guess matches secret code or player has run out of turns

  // turnsLeft () => maxTurns - guesses

  // reset/restart game
    // generate new code
    // clear guesses

  return (
    <div>
      GAME
    </div>
  )

};

export default Game;
